services:
  app:
    container_name: nova-app
    image: node:20
    working_dir: /app
    stdin_open: true
    tty: true
    command: tail -f /dev/null
    ports:
      - "5173:5173"
    volumes:
      - ./src:/app
      - ./docs:/output
    restart: unless-stopped